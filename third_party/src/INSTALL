--------------------------------------------------------------------------------
x264
--------------------------------------------------------------------------------
% cd third_party/src
% tar zxvf x264.tar.gz
% ./configure  --prefix=`pwd`/../.. --enable-pthread
% make
% make install


--------------------------------------------------------------------------------
ffmpeg (Notes: x264 must be built and installed first)
--------------------------------------------------------------------------------
% cd third_party/src
% tar zxvf ffmpeg.tar.gz
% cd ffmpeg

# USE THE FOLLOWING FOR LINUX
% ./configure --prefix=`pwd`/../.. --enable-gpl --enable-swscaler --enable-pthreads --enable-libx264 --extra-cflags=-I`pwd`/../../include --extra-ldflags=-L`pwd`/../../lib

# USE THE FOLLOWING FOR MAC OS X
./configure --prefix=`pwd`/../.. --enable-gpl --enable-swscaler --enable-pthreads --enable-libx264 --extra-cflags=-I`pwd`/../../include --extra-ldflags=-L`pwd`/../../lib --disable-vhook --disable-mmx

% make
% make install
